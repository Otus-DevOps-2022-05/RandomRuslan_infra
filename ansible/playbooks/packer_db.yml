- name: Install mongodb

  hosts: all
  become: true

  tasks:

    - name: mongodb
      apt:
        name: mongodb

    - name: start mongodb
      service: name=mongodb state=started

    - name: enable mongodb
      service:
        name: mongodb
        enabled: yes
