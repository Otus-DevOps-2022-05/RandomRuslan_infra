- name: Install mongodb

  hosts: 127.0.0.1
  become: true

  tasks:

    - name: mongodb
      apt:
        name: mongodb

    - name: start mongodb
      service: name=mongodb state=started

    - name: enable mongodb
      service:
        name: mongodb
        enabled: yes
